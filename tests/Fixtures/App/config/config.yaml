framework:
    test: true
    secret: secret

doctrine:
    dbal:
        driver: pdo_sqlite
        path: ':memory:'
        memory: true
    orm:
        auto_generate_proxy_classes: '%kernel.debug%'
        naming_strategy: doctrine.orm.naming_strategy.underscore
        auto_mapping: true
